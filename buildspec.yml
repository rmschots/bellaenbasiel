version: 0.2
phases:
  install:
    commands:
    - echo Installing source NPM dependencies...
    - npm --version
    - node --version
    - npm install
  build:
    commands:
    - echo Build started on `date`
    - npm run build
artifacts:
  files:
  - '**/*'
  base-directory: 'dist*'
